set plugin-path "/opt/homebrew/lib/zathura"
# --- General behavior ---
# Open PDFs in best-fit mode (fit width/height)
set adjust-open best-fit

# Step size for zooming (%)
set zoom-step 10

# One page per row by default
set pages-per-row 1

# --- Navigation ---
# Vim-like scrolling
map j scroll down
map k scroll up
map d scroll half-down
map u scroll half-up

# Page navigation
map J navigate next
map K navigate previous
map gg goto top
map G goto bottom

# --- Search ---
map n search next
map N search previous

